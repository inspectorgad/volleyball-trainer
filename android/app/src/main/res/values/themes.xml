<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Must descend from Theme.AppCompat. androidbrowserhelper's LauncherActivity
      extends AppCompatActivity, which throws
        IllegalStateException: You need to use a Theme.AppCompat theme
      during onCreate under any other theme — an immediate crash on launch.

      Only visible for the instant before Chrome takes over, so it just has to
      match the splash rather than style any real UI.
    -->
    <style name="Theme.App" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@color/colorPrimary</item>
        <item name="android:navigationBarColor">@color/colorPrimary</item>
        <item name="android:windowBackground">@color/backgroundColor</item>
    </style>
</resources>
